name: Build and Push GERP (Docker Hub)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Docker tag to publish (e.g., 2.1)"
        required: false
        default: ""
  push:
    paths:
      - "workflow/docker/gerp/**"

env:
  IMAGE_NAME: gerp

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Determine tag
        id: determine-tag
        shell: bash
        run: |
          TAG="${{ github.event.inputs.tag }}"
          if [ -z "$TAG" ]; then
            TAG="${{ vars.DOCKERHUB_TAG }}"
          fi
          if [ -z "$TAG" ] && [ -f workflow/docker/gerp/TAG ]; then
            TAG="$(cat workflow/docker/gerp/TAG | tr -d '[:space:]')"
          fi
          if [ -z "$TAG" ]; then
            TAG="2.1"
          fi
          echo "TAG_NAME=$TAG" >> "$GITHUB_ENV"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: docker.io/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ env.TAG_NAME }}
            type=ref,event=branch
            type=sha,format=short
          labels: |
            org.opencontainers.image.title=gerp
            org.opencontainers.image.source=${{ github.repositoryUrl }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./workflow/docker/gerp
          file: ./workflow/docker/gerp/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
