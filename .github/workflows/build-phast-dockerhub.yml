name: Build and Push phast (Docker Hub)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Docker tag to publish (e.g., 1.5)"
        required: false
        default: ""
  push:
    paths:
      - "workflow/docker/phast/**"

env:
  IMAGE_NAME: phast

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Determine tag
        id: determine-tag
        shell: bash
        run: |
          TAG="${{ github.event.inputs.tag }}"
          if [ -z "$TAG" ]; then
            TAG="${{ vars.DOCKERHUB_TAG }}"
          fi
          if [ -z "$TAG" ] && [ -f workflow/docker/phast/TAG ]; then
            TAG="$(cat workflow/docker/phast/TAG | tr -d '[:space:]')"
          fi
          if [ -z "$TAG" ]; then
            TAG="1.5"
          fi
          echo "TAG_NAME=$TAG" >> "$GITHUB_ENV"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: docker.io/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ env.TAG_NAME }}
            type=ref,event=branch
            type=sha,format=short
          labels: |
            org.opencontainers.image.title=phast
            org.opencontainers.image.source=${{ github.repositoryUrl }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./workflow/docker/phast
          file: ./workflow/docker/phast/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
